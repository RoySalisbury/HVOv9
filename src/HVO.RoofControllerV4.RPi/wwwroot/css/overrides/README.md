# Roof Controller CSS Overrides

Global styling lives in the shared `HVO.WebSite.Themes` project. Use this directory for site-specific refinements (background imagery, layout tweaks) that do not belong in the shared theme. Keep overrides minimal and prefer theme tokens for colors and spacing.
