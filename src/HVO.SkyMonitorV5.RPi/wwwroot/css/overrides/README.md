# SkyMonitor v5 CSS Overrides

Shared theme assets live in the `HVO.WebSite.Themes` Razor Class Library. Use this directory for per-page or per-site override styles that cannot live in the shared theme. Keep overrides small and document context inline when possible.
