name: .NET Build & Test

on:
  push:
    branches: [ main, feature/reorganize-project-structure ]
    paths:
      - 'src/HVO/**'
      - 'src/HVO.DataModels/**'
      - 'src/HVO.SourceGenerators/**'
      - 'src/HVO.WebSite.Themes/**'
      - 'src/NuGet.config'
      - 'src/global.json'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - '.github/workflows/dotnet.yml'
  pull_request:
    branches: [ main, feature/** ]
    paths:
      - 'src/HVO/**'
      - 'src/HVO.DataModels/**'
      - 'src/HVO.SourceGenerators/**'
      - 'src/HVO.WebSite.Themes/**'
      - 'src/NuGet.config'
      - 'src/global.json'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - '.github/workflows/dotnet.yml'
  schedule:
    - cron: '0 2 * * *'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Job to build all projects that do not target iOS, using the Linux runner.
  build-non-maui:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/*.sln*
            global.json
            NuGet.config

      - name: Restore dependencies
        # Restore the full root solution (benchmarks are excluded from this .sln)
        run: cd src && dotnet restore HVOv9.sln --nologo -v minimal

      - name: Build all non-MAUI projects (Debug)
        # Build the full root solution
        run: cd src && dotnet build HVOv9.sln --no-restore -c Debug -v minimal --nologo -maxcpucount /p:ContinuousIntegrationBuild=true

      - name: Upload build artifacts (Debug)
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-non-maui
          path: |
            src/**/bin/Debug/**
            !src/**/bin/Debug/**/ref/**
            src/**/obj/Debug/net9.0/compressed/**
            src/**/obj/Debug/net9.0/scopedcss/**
          retention-days: 1

  test-unit:
    needs: build-non-maui
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    strategy:
      matrix:
        test-project:
          - HVO.Iot/HVO.Iot.Devices.Tests/HVO.Iot.Devices.Tests.csproj
          - HVO.WebSite/HVO.WebSite.Playground.Tests/HVO.WebSite.Playground.Tests.csproj
          - HVO.RoofControllerV4/HVO.RoofControllerV4.RPi.Tests/HVO.RoofControllerV4.RPi.Tests.csproj

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/*.sln*
            global.json
            NuGet.config

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts-non-maui
          path: src

      - name: Run unit tests
        run: |
          cd src
          mkdir -p TestResults/${{ matrix.test-project }}
          dotnet test ${{ matrix.test-project }} --no-build -c Debug --nologo -v minimal \
            --filter "TestCategory!=Integration" \
            -l "trx;LogFileName=${{ matrix.test-project }}.trx" \
            --results-directory TestResults/${{ matrix.test-project }} \
            --logger "console;verbosity=normal" \
            --settings coverage.runsettings

      - name: Upload Unit Test Results and Coverage
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: test-results-unit-${{ matrix.test-project }}-${{ github.run_id }}
          path: |
            src/**/TestResults/*/coverage.cobertura.xml
            src/**/TestResults/*.trx
          if-no-files-found: warn
          retention-days: 7

  test-integration:
    needs: build-non-maui
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    if: |
      github.ref == 'refs/heads/main' || 
      github.event_name == 'schedule' || 
      contains(github.event.pull_request.labels.*.name, 'integration-tests')
    
    strategy:
      matrix:
        test-project:
          - HVO.Iot/HVO.Iot.Devices.Tests/HVO.Iot.Devices.Tests.csproj
          - HVO.WebSite/HVO.WebSite.Playground.Tests/HVO.WebSite.Playground.Tests.csproj
          - HVO.RoofControllerV4/HVO.RoofControllerV4.RPi.Tests/HVO.RoofControllerV4.RPi.Tests.csproj

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/*.sln*
            global.json
            NuGet.config

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts-non-maui
          path: src

      - name: Run integration tests
        run: |
          cd src
          mkdir -p TestResults/${{ matrix.test-project }}
          dotnet test ${{ matrix.test-project }} --no-build -c Debug --nologo -v minimal \
            --filter "TestCategory=Integration" \
            -l "trx;LogFileName=${{ matrix.test-project }}.trx" \
            --results-directory TestResults/${{ matrix.test-project }} \
            --logger "console;verbosity=normal" \
            --settings coverage.runsettings

      - name: Upload Integration Test Results and Coverage
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: test-results-integration-${{ matrix.test-project }}-${{ github.run_id }}
          path: |
            src/**/TestResults/*/coverage.cobertura.xml
            src/**/TestResults/*.trx
          if-no-files-found: warn
          retention-days: 7

  benchmark-smoke:
    continue-on-error: true
    needs: build-non-maui
    runs-on: ubuntu-latest
    timeout-minutes: 10

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
      DOTNET_ENVIRONMENT: Benchmark
      HVO_BENCH_WARMUP_COUNT: 1
      HVO_BENCH_ITERATION_COUNT: 1
      HVO_BENCH_LAUNCH_COUNT: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/*.sln*
            global.json
            NuGet.config

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts-non-maui
          path: src

      - name: Run benchmark smoke suite (Debug)
        working-directory: src
        run: |
          dotnet run --project HVO.SkyMonitorV5/HVO.SkyMonitorV5.RPi.Benchmarks/HVO.SkyMonitorV5.RPi.Benchmarks.csproj -c Debug --no-build -- --filter '*EndToEndPipeline*'

      - name: Upload benchmark summary
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results-${{ github.run_id }}
          path: |
            src/HVO.SkyMonitorV5/HVO.SkyMonitorV5.RPi.Benchmarks/BenchmarkDotNet.Artifacts/results/*EndToEndPipeline*.md
            src/HVO.SkyMonitorV5/HVO.SkyMonitorV5.RPi.Benchmarks/BenchmarkDotNet.Artifacts/results/*EndToEndPipeline*.csv
          if-no-files-found: warn
          retention-days: 7
